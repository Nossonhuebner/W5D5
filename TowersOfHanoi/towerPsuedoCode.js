class TowersOfHanoi {

  constructor() {
    // instantiates towers array with discs in starting position
  }

  run() {
    // until gameover
    // prompt player for move
    // check if valid move, make move if valid
  }

  getMove() {
    // prompt player for move
    // returns move as string
  }

  parseMove(move) {
    // converts move from strings to array of integers
  }

  move(start, end ) {
    // moves disc from start to end
  }

  isValidMove(start, end) {
    // checks that start and end are on the board
    // checks that start has discs to move
    // checks that moved disc is smaller than last end disc || end is empty
  }

  isGameOver() {
    // checks that all discs are all on one tower other than starting tower
  }



}
